{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Kotonia Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "${workspaceFolder}/..",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
